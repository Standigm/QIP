# @package _global_

defaults:
  - /system: encoder_train.yaml
  # override
  # system config
  - /system/encoder_config: "gps/medium.yaml"
  - /system/task_head_configs: gps/MT0.yaml
  - /system/optimizer_configs: lr_t1
  # train config
  - override /trainer:
      [
        "default",
        "accelerator/gpu",
        "devices/4",
        "max_steps/10k",
        "strategy/ddp",
        "accumulate_grad_batches/4.yaml",
        "precision/32"
      ]
  - override /callbacks: default.yaml
  - override /datamodule: "gps/MT0.yaml"
  - override /logger: ["csv", "wandb"]

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters
callbacks:
  model_checkpoint:
    monitor: val/score
    mode: max
trainer:
  accumulate_grad_batches: 1
  log_every_n_steps: 16
  val_check_interval: 16
datamodule:
  batch_size: 24
name: gps_MT_HD_3
seed: 8272
system:
#   checkpoint_path: null
#   trainable_modules: null
#   freeze_modules: null

  encoder_config:
    # module:
    #   use_independent_token: True
    state_path: /db2/users/jungwookim/projects/admet_prediction/outputs/train/runs/gps_PTHD/2023-09-04/01-38-15/checkpoints/checkpoint_000114872/encoder.pt


  # optimizer_configs:
  #   - optimizer:
  #       _target_: torch.optim.AdamW
  #       lr: 1e-5 # use very small lr with CosineAnnealingWarmUpRestartsWithDecay
  #       # params: null # null or '' for all parameters
  #       # modules: null # null or '' for all module parameters
  #     lr_scheduler:
  #       scheduler:
  #         _target_: qip.modules.lr_scheduler.CosineAnnealingWarmUpRestartsWithDecay
  #         T_0: 10_000
  #         T_mult: null
  #         eta_max: 15e-5
  #         T_up: 2000
  #         gamma: 1.0
  #         warmup_base_lr: 0.0
  #       interval: "step"
  #       frequency: 1