# @package _global_

defaults:
  - /system: encoder_train.yaml
  # override
  # system config
  - /system/encoder_config: "gps/gnn_gine.yaml"
  - /system/task_head_configs: gps/task_head_default/TDC/half_life_obach.yaml
  - /system/optimizer_configs: paper_finetuning.yaml
  # train config
  - override /trainer:
      [
        "default",
        "accelerator/gpu",
        "devices/2",
        "max_steps/2k",
        "accumulate_grad_batches/4",
      ]

  - override /callbacks: default.yaml
  - override /datamodule: [gps/multidata_default.yaml, gps/dataset_configs/TDC/half_life_obach/v1_0_0]
  - override /logger: ["csv"]
callbacks:
  model_checkpoint:
    monitor: val/SpearmanCorrCoef/half_life_obach.half_life_obach
    mode: max
trainer:
  devices: 1
  max_epochs: 5
  max_steps: -1
  accumulate_grad_batches: 1
  log_every_n_steps: 2
  val_check_interval: 2
datamodule:
  batch_size: 16
# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters
name: gps_PTNAME_finetuning_half_life_obach
seed: 8282
system:
  checkpoint_path: /db2/users/jungwookim/projects/admet_prediction/outputs/train/runs/gps_MT0/2023-07-24/12-25-37/checkpoints/checkpoint_000002820.ckpt
  
  frozen_modules:
    - encoder




  task_head_configs:
    half_life_obach:
      weight: 1

