# @package trainer

defaults:
  - default.yaml

# execute 10 steps
devices: 1
accelerator: auto
# strategy: ddp
# fast_dev_run: False
max_epochs: null
max_steps: 10
accumulate_grad_batches: 4
val_check_interval: 40 # max_steps * accumulate_grad_batches
log_every_n_steps: 5
# num_sanity_val_steps: 2
# limit_train_batches: 1.0
# limit_val_batches: 1.0
# limit_test_batches: 1.0
# track_grad_norm: -1
